cd <directory>
     |	Change the current default directory to <directory>

clr
     |	Clear the screen

dir <directory>
     |	List the contents of directory <directory>

environ
     |	List all the environment strings

echo <comment>
     |	Display <commenfgnmj,.,mnbvcvbnm,.t> on the display followed by a new line

help
     |	Display the user manual

pause
     |	 Pause operation of the shell until 'Enter' is pressed

quit
     |	Quit the shell